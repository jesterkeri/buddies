# =============================================================
# Buddies — Environment Variables
# =============================================================

# AI providers are configured from the frontend Connect tab.
# No env vars needed for AI — users connect their own API keys.

# Server Configuration
SERVER_PORT=3000
NODE_ENV=development
